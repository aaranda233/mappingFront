// Copia este archivo como env.js y ajusta los valores para tu entorno local
// Este archivo NO se sube al repo (está en .gitignore)
// En Kubernetes, env.js se inyecta vía ConfigMap

window.env = {
  IP_BACKEND: "localhost:5008",
  IP_LANGCHAIN: "localhost:8001",
  VERSION: "dev"
};
